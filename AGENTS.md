# AGENTS.md

This file is a bootstrap pointer for Codex.

`CLAUDE.md` is the single source of truth for all agent instructions in this repository.

Before making changes, read `CLAUDE.md`, then read the relevant sections of `docs/PRD.md` and `docs/ROADMAP.md` for the current task.

Do not copy rules from `CLAUDE.md` into this file. Keep this file as a pointer only, so agent instructions do not drift.
